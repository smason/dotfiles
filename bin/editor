#!/bin/sh

# open the file(s)
if emacsclient --alternate-editor vim "$@"; then
    # switch focus back to terminal here when file is closed
    xdotool windowactivate "$WINDOWID" windowfocus "$WINDOWID"
fi
