#/bin/sh

# Python's simple HTTP server
#  -m SimpleHTTPServer tells python to run that module
#  8080 is the only parameter for the module, i.e. the port number
# exec python -m SimpleHTTPServer 8080

# use the fancy webfs program
#  -ss should make it more verbose
#  -F should stop it being a daemon
#  -p 8080 tells it to use port 8080
exec webfsd -ssFp 8088
